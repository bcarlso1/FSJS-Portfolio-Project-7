import React from 'react';

// Shows when no results for search
const NotFound = () => {
    return (
        <li class="not-found">
        <h3>No Results Found</h3>
        <p>You search did not return any results. Please try again.</p>
        </li>
    )
   
};

export default NotFound;